Path to service = /etc/systemd/system/
sudo rm startalexa.service startpagekite.service
Copy service file
sudo cp startalexa.service /etc/systemd/system/
sudo cp startpagekite.service /etc/systemd/system/

Execute below commands 
1. Reload daemon
sudo systemctl daemon-reload 

2.Start service manually for checking
sudo systemctl start startalexa.service
sudo systemctl start startpagekite.service

3.Check if services have started
sudo ps -ax | grep python

4. if started
sudo systemctl stop startalexa.service
sudo systemctl stop startpagekite.service

5. Enable ctl to start on bootup
sudo systemctl enable startalexa.service
sudo systemctl enable startpagekite.service

6.To check if services have started after bootup
sudo ps -ax | grep python

7. Debug 
systemctl | grep start

8.Restart service explicitly
sudo ps -ax | grep python  //Returns id of processes
sudo kill <pid> <pid>

https://www.youtube.com/watch?v=ENjRzQkqEVA
